import { View, Text } from "react-native";
import style from "../style.js";

export default function ExerciseDetail({ navigation }) {
  return (
    <View style={style.appView}>
      <Text style={style.title}>ExerciseDetail</Text>
    </View>
  );
}
